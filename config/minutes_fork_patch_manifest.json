{
  "schemaVersion": 1,
  "upstreamRepository": "signalapp/ringrtc",
  "upstreamTag": "v2.70.2",
  "upstreamPatches": [
    {
      "path": ".gitattributes",
      "purpose": "Treat the vendored WebRTC patch queue as text with stable line endings."
    },
    {
      "path": ".gitignore",
      "purpose": "Ignore Python bytecode generated by Minutes maintenance tooling."
    },
    {
      "path": ".github/workflows/ringrtc.yml",
      "purpose": "Use public GitHub-hosted runners instead of Signal-only larger-runner labels."
    },
    {
      "path": "bin/build-desktop",
      "purpose": "Build and exercise the fork-owned post-APM WebRTC processor test."
    },
    {
      "path": "bin/gsync-webrtc",
      "purpose": "Apply the Minutes WebRTC patch queue after synchronizing WebRTC sources."
    },
    {
      "path": "src/node/index.ts",
      "purpose": "Export the versioned Minutes recording-tap API types."
    },
    {
      "path": "src/node/package-lock.json",
      "purpose": "Lock the renamed compatibility package and its Minutes distribution dependencies."
    },
    {
      "path": "src/node/package.json",
      "purpose": "Publish @minutes/ringrtc and install checksum-pinned Minutes native prebuilds."
    },
    {
      "path": "src/node/ringrtc/Service.ts",
      "purpose": "Expose thin Node bindings for audio and outgoing screen-share video taps."
    },
    {
      "path": "src/node/scripts/fetch-prebuild.js",
      "purpose": "Remove the upstream artifact downloader from the Minutes compatibility package."
    },
    {
      "path": "src/node/scripts/prepublish.js",
      "purpose": "Validate the Minutes package identity, API version, assets, and checksums before release."
    },
    {
      "path": "src/rust/src/electron.rs",
      "purpose": "Add the Electron FFI boundary for versioned bounded audio/video recording taps."
    },
    {
      "path": "src/rust/src/lib.rs",
      "purpose": "Register the fork-owned audio_tap and video_tap Rust modules."
    },
    {
      "path": "src/rust/src/webrtc/audio_device_module.rs",
      "purpose": "Install the post-APM local audio processor and remote playout observer hooks."
    },
    {
      "path": "src/rust/src/webrtc/ffi/audio_device_module.rs",
      "purpose": "Expose the WebRTC audio processor callback required by the post-APM tap."
    },
    {
      "path": "src/rust/src/webrtc/peer_connection_factory.rs",
      "purpose": "Wire fork-owned tap state into the existing WebRTC factory without recording policy."
    }
  ],
  "forkOwnedPaths": [
    {
      "pattern": ".github/workflows/minutes_*.yml",
      "purpose": "Minutes-only build, release, and upstream synchronization automation."
    },
    {
      "pattern": "CONTRIBUTING-MINUTES.md",
      "purpose": "Contribution rules for the maintained fork."
    },
    {
      "pattern": "MINUTES-CHANGELOG.md",
      "purpose": "Fork release notes kept separate from upstream RingRTC history."
    },
    {
      "pattern": "README-MINUTES.md",
      "purpose": "Fork architecture, release, and maintenance entry point."
    },
    {
      "pattern": "bin/apply-webrtc-patches.py",
      "purpose": "Idempotently apply the external WebRTC patch queue."
    },
    {
      "pattern": "bin/minutes_*.py",
      "purpose": "Minutes artifact, version, patch-manifest, and upstream tooling."
    },
    {
      "pattern": "bin/tests/test_apply_webrtc_patches.py",
      "purpose": "Tests for the external WebRTC patch queue."
    },
    {
      "pattern": "bin/tests/test_minutes_*.py",
      "purpose": "Tests for Minutes manifests and maintenance tooling."
    },
    {
      "pattern": "bin/verify_minutes_node_addon.js",
      "purpose": "Release-time ABI and tap API verification."
    },
    {
      "pattern": "config/minutes_*.json",
      "purpose": "Machine-readable fork baseline, patch inventory, and release targets."
    },
    {
      "pattern": "docs/FORK-MAINTENANCE.md",
      "purpose": "Human workflow for syncing Signal RingRTC safely."
    },
    {
      "pattern": "docs/MINUTES-PATCHES.md",
      "purpose": "Generated human view of the machine-readable patch manifest."
    },
    {
      "pattern": "docs/superpowers/specs/2026-07-31-post-apm-audio-tap-design.md",
      "purpose": "Design record for the post-APM outgoing audio capture boundary."
    },
    {
      "pattern": "patches/webrtc/*.patch",
      "purpose": "Minutes changes to Signal WebRTC stored outside the WebRTC checkout."
    },
    {
      "pattern": "src/node/prebuilds.json",
      "purpose": "Generated checksum manifest template for Minutes release assets."
    },
    {
      "pattern": "src/node/scripts/fetch-minutes-prebuild.js",
      "purpose": "Checksum-verifying installer for Minutes native release assets."
    },
    {
      "pattern": "src/node/scripts/fetch-minutes-prebuild.test.js",
      "purpose": "Distribution installer tests."
    },
    {
      "pattern": "src/node/scripts/prepublish.test.js",
      "purpose": "Minutes release metadata validation tests."
    },
    {
      "pattern": "src/node/test/AudioTap-test.ts",
      "purpose": "Node audio tap contract tests."
    },
    {
      "pattern": "src/node/test/VideoTap-test.ts",
      "purpose": "Node outgoing-video tap contract tests."
    },
    {
      "pattern": "src/rust/src/audio_tap.rs",
      "purpose": "Bounded lock-free audio tap implementation owned by Minutes."
    },
    {
      "pattern": "src/rust/src/video_tap.rs",
      "purpose": "Bounded latest-frame outgoing screen-share tap owned by Minutes."
    },
    {
      "pattern": "src/rust/tests/audio_tap.rs",
      "purpose": "Rust audio tap behavior and contention tests."
    },
    {
      "pattern": "src/rust/tests/video_tap.rs",
      "purpose": "Rust video tap lifecycle and frame format tests."
    }
  ]
}
